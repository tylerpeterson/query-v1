

module.exports = {
  // TODO export a strategy that opens a url when triggered
  // TODO export a strategy that rejects the promise when triggered (allows app users to err out and redirect to the token flow)
};